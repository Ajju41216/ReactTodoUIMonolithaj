trigger: none
pool: HLD
steps:
- task: NodeTool@0
  displayName: To install Nodejs
  inputs:
    versionSource: 'spec'
    versionSpec: '16.x'
- task: PowerShell@2
  displayName:  NPM install
  inputs:
    targetType: 'inline'
    script: 'npm install'
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: 'npm run build'
- task: ArchiveFiles@2
  displayName: 
  inputs:
    rootFolderOrFile: '$(SystemDefaultWorkingDirectory)/build/'
    includeRootFolder: true
    archiveType: 'zip'
    archiveFile: '$(SystemDefaultWorkingDirectory)/build/'
    replaceExistingArchive: true
- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(SystemDefaultWorkingDirectory)/build.zip'
    ArtifactName: 'to-do'
    publishLocation: 'Container'